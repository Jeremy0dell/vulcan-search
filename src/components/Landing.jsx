import React from 'react'

const Landing = () =>
  <div id="landing">
    <h1>Thank you very much for this opportunity!</h1>
    <div>Use the Navbar at the top to browse through my solutions</div>
    <div>Note: On the Heatmap, I sorted the data by weekday order (instead of alphabetical)</div>
  </div>

export default Landing
